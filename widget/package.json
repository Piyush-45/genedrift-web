{
  "name": "genedrift-website-widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:fields": "node scripts/sync-fields.mjs",
    "dev": "npm run sync:fields && vite",
    "build": "npm run sync:fields && tsc -b && vite build",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^5.9.2",
    "vite": "^8.3.0"
  }
}
