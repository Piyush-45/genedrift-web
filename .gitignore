node_modules/
.next/
out/
.env*
!.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Catalyst website service build output
services/catalyst-website/node_modules
services/catalyst-website/dist
genedrift-website-appsail-*.zip

# Widget build output
widget/node_modules
widget/app
widget/preview/assets
widget/*.tsbuildinfo
genedrift-website-widget-*.zip

# Working folder for downloaded screenshots and scratch copies. Never in the
# repo: the name alone discloses tooling, and the contents are duplicates of
# files that already live in creator-seed/ and context/.
Claude outputs/
